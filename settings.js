const settings = {
  // surprisingly, the performance is better *with* glow
  glow: true,
  
  // true: stroke
  // false: two-shape
  //sts: false,
  
  // musically-synchronized light flickers
  flicker: true,
  
  // speed of the player
  speed: 250,
}